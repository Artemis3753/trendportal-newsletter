# 트렌드포털 뉴스레터 웹사이트 PRD

| | |
|---|---|
| **작성자** | 오지환 |
| **클라이언트** | <https://www.instagram.com/trend__portal/> |
| **버전** | v0.9 |
| **최종 수정** | 2026-09-25 |

---

## 1. 배경

현재 이 채널이 독자와 만나는 주 창구는 인스타그램이다. 카드뉴스를 꾸준히 발행하고 있지만, 새로운 독자에게 닿는 경로가 인스타그램에 고정되어 있어 구독자 풀을 넓힐 수단이 없다.

> 2026-09-21 확인: **네이버 블로그(<https://m.blog.naver.com/trendportal>)도 운영하고 있다.** 다만 블로그도 남의 플랫폼이라, 아래 문제(자체 창구와 구독자 명단이 없음)는 그대로다. 블로그의 반복 시리즈는 사이트 콘텐츠의 재료가 된다 (Q-35).

또한 발행한 콘텐츠가 플랫폼 안에만 남기 때문에, 지난 글을 찾아보거나 외부에서 검색으로 유입되는 경로가 없다. 독립된 웹사이트는 이 두 가지를 동시에 해결하는 창구가 된다.

---

## 2. 목표

인스타그램 외의 유입 경로를 만들어 구독자 풀을 넓히고, 웹사이트에 일정 수 이상의 정기 방문자를 확보한다. 확보한 방문자 수를 근거로 브랜드 광고를 유치한다.

### 2.1 성공 기준

게시글당 조회수 1,000 이상을 달성하고, 광고 문의를 기존 대비 10% 늘린다.

| 지표 | 목표치 | 어떻게 측정하는가 |
|---|---|---|
| 게시글당 조회수 | 1,000 이상 | 조회수 집계 (F-10) |
| 광고 문의 건수 | 기존 대비 10% 증가 | 클라이언트가 직접 집계 |
| 이메일 구독자 수 | *미정 (Q-13)* | 스티비 주소록 |

> 위 두 목표치는 **비교 기준과 측정 기간이 아직 정해지지 않았다** (Q-16). "조회수 1,000"이 발행 후 한 달 기준인지 누적인지, "10% 증가"가 무엇 대비인지에 따라 달성 여부가 달라진다.

### 2.2 목표가 아닌 것

- **인스타그램을 대체하는 것.** 인스타그램은 계속 주력 채널로 유지된다. 웹사이트는 창구를 하나 더하는 것이지 옮기는 것이 아니다.
- **웹사이트 자체로 수익을 내는 것.** 사이트에 광고를 붙이거나 유료 콘텐츠를 파는 계획은 없다. 사이트는 광고를 유치하기 위한 자산이지 수익원이 아니다.
- **회원제 커뮤니티를 만드는 것.** 회원 가입·로그인·포럼은 이번 범위가 아니다.
  - v0.8까지는 댓글까지 제외했지만, 2026-09-18 클라이언트 요청으로 **글 댓글과 제보 게시판은 비회원 방식으로 넣었다** (v0.9, Q-33 해결). 회원 기능은 여전히 제외한다.

---

## 3. 사용자

### 3.1 운영자 (클라이언트)

인스타그램 트렌드 매거진을 운영하는 1인 운영자. 노션을 오래 사용해 왔고 익숙하다.

**하는 일**

- **카드뉴스를 매일** 발행한다
- **뉴스레터를 매주 금요일** 따로 써서 보낸다. 뉴스레터는 카드뉴스를 옮긴 것이 아니라 **별도로 작성하는 독립 콘텐츠**다 (2026-09-18 미팅)
  - v0.8까지는 "카드뉴스 매주 금요일, 뉴스레터 주 1~2회"로 적었다. 사이트 문구(소개 페이지·구독 칸)와 어긋나서, **v0.9에서 사이트 문구를 기준으로 맞췄다** (2026-09-25)
- 사이트용 글의 재료로 **글자 없는 사진과 스크립트(카드별 문구)**를 넘긴다. 사이트 글은 작성자가 대신 쓰거나 운영자가 직접 쓴다 (2026-09-18 미팅)
- 브랜드 / 트렌드 / 광고 / 인터뷰 / 뉴스 다섯 갈래의 콘텐츠를 다룬다 (`뉴스`는 v0.9에 추가, 정의는 Q-34)
- 인스타그램 외에 **네이버 블로그**도 운영한다. 블로그에는 "이번 주 마케팅 이슈 한 번에 총정리"처럼 번호를 붙인 반복 시리즈가 있다
- 광고주와 직접 소통한다

**무엇이 편해야 계속 쓰는가**

- **글을 쓰고 미디어를 올리는 곳이 한 곳이어야 한다.** 글과 미디어를 서로 다른 도구에 나눠 올리면 한쪽을 빠뜨린다.
- **발행 절차가 짧고 매주 같아야 한다.** 사이트 글은 WordPress에서, 뉴스레터는 스티비에서 쓴다. 두 콘텐츠가 서로 다르므로 두 도구로 나뉘는 것은 자연스럽다 (8장, 10장).

> v0.6까지는 "새 도구를 배우지 않아도 되어야 한다"를 이유로 노션을 입력 도구로 삼았다. v0.7에서 **WordPress 관리자 화면이라는 새 도구를 배우는 부담**을 받아들이는 대신, 노션·드라이브·자체 코드로 이어 붙인 구조의 운영 부담을 없앴다.

**이 사람이 하지 않아야 하는 일**

- 코드를 만지는 일
- 이미지 크기를 맞추거나 HTML을 다루는 일
- 구독자 명단을 직접 관리하는 일

### 3.2 독자

인스타그램에서 이 채널의 카드뉴스를 보는 사람. 대부분 모바일로 접근한다.

**어디서 오는가**

- 인스타그램 프로필의 링크
- 인스타그램 게시물의 "링크는 프로필에" 형태의 안내
- 검색엔진 — 웹사이트를 만든 뒤 새로 생기는 경로

**무엇을 하는가**

- 관심 있는 주제의 글을 찾아 읽는다
- 지난 글을 다시 찾아본다 — 인스타그램에서는 어려운 일이다
- 마음에 들면 이메일 구독을 신청한다

**무엇이 있어야 하는가**

- 모바일에서 읽기 편한 화면
- 지난 글을 찾을 수 있는 수단 (카테고리 필터, 검색)
- 구독까지 가는 짧은 경로

---

## 4. 범위

### 4.1 포함하는 것

1. 글 목록 페이지 (카드 그리드, 최신순)
2. 글 상세 페이지 — **이미지와 영상을 모두 표시**
3. 카테고리 필터
4. 사이트 내 검색
5. WordPress에 글·미디어 입력
6. 이미지 최적화
7. 스티비 뉴스레터 작성·발송 (**수동**, 사이트 글과 별개 콘텐츠)
8. 이메일 구독 폼 (스티비 폼)
9. 조회수 집계
10. 모바일 반응형 웹
11. SEO 기본 + OG 태그
12. 개인정보처리방침 + 수신동의 문구
13. **글 댓글** — 비회원(이름·이메일) 방식 (v0.9, Q-33)
14. **제보 게시판** — KBoard 기반, 비회원 작성 (v0.9, Q-33)
15. **카카오톡 공유 버튼** — 글 본문 끝 (v0.9)
16. 광고·협업 문의 페이지 (v0.9. v0.8까지 "광고 문의 경로는 보류"였다)

### 4.2 포함하지 않는 것

> ★ 이 문서에서 가장 중요한 칸. "빼먹은 것"과 "알면서 뺀 것"은 완전히 다른데, 문서에 없으면 구분할 방법이 없다.

| 항목 | 왜 빼는가 |
|---|---|
| 좋아요 | 독자 반응은 인스타그램에서 이미 받고 있다. WordPress.com(Jetpack)이 새 글마다 좋아요·공유 버튼을 **기본으로 켜기** 때문에 글마다 끄고 있다 (10장). *댓글은 v0.9에서 포함으로 옮겼다 (4.1, Q-33)* |
| Jetpack 공유 버튼 | 카카오톡이 없다. 공유는 직접 만든 카카오톡 버튼 하나로 한다 (F-15) |
| 구독 취소 페이지 | 스티비가 제공하는 것을 사용한다. 법적 요건이 걸린 기능을 직접 만들면 틀렸을 때 책임이 이쪽으로 온다 |
| 운영자용 관리자 페이지 개발 | WordPress 관리자 화면이 이 역할을 한다 |
| 뉴스레터 자동 발송 | 뉴스레터를 스티비에서 따로 쓰므로 "사이트 글 발행 → 메일 발송"을 이어 붙일 대상이 없다 (v0.8). 참고로 스티비 이메일 API는 프로 요금제(월 29,000원~)부터다(부록 A.1) |
| WordPress 자체 뉴스레터 기능 | 해외 기능이라 정보통신망법 요건을 직접 챙겨야 한다. Kit·beehiiv를 기각한 이유와 같다 (8장). 또한 **글을 발행하면 그 글이 곧 메일로 나가는 구조**라, 뉴스레터를 별도로 쓰는 운영 방식(5.1)과 맞지 않는다 (v0.8) |
| 다크모드 | 색 체계를 두 벌 관리해야 한다. 디자인이 안정된 뒤로 미룬다 |
| 다국어 | 독자가 한국어권이다 |
| 소셜 로그인 / 회원 기능 | 구독은 이메일 주소 하나면 된다. 계정을 만들면 비밀번호 관리 책임이 생긴다. 댓글·제보도 비회원(이름·이메일)으로 충분하다고 봤다 (v0.9) |
| 스티비 연동 자동화 | 스티비 공식 MCP는 없고, 이메일 생성·발송 API는 프로 요금제부터다. 뉴스레터는 주 1회 수준이라 발송을 자동화해 아끼는 시간이 작다. 재검토 여부는 Q-39 |
| 영상 재인코딩 | 구현 난이도가 높고 이 프로젝트 규모를 넘는다. 배포 후 실측해서 다시 판단한다 (Q-22) |

### 4.3 미디어 처리 방식

2026-09-16에 실측한 실제 콘텐츠 14건(부록 D)을 근거로 한다.

- **글과 미디어를 모두 WordPress에 올린다.** WordPress.com은 파일 하나당 최대 2GB까지 안정적으로 업로드된다고 안내한다(부록 A.1).
- **사이트 글은 "글자 없는 사진 + 진짜 텍스트"로 만든다** (2026-09-18 결정). 카드마다 `사진 → 소제목 → 설명`을 반복하고 갤러리 블록은 쓰지 않는다. 카드뉴스 첫 장(커버)의 문구는 글 제목이 되고, 커버 사진은 대표 이미지가 된다. 본문에 중복해서 넣지 않는다. 이미지 속 글자가 검색되지 않는 문제(7.2)가 이 방식으로 해결된다. 대가로 **운영자는 인스타그램용(글자 있음)과 사이트용(글자 없음) 이미지를 두 벌 만들어야 한다.**
- **글 상단 커버에는 사진을 쓰지 않는다** (2026-09-23 결정). 카드뉴스 표지에는 제목 글자가 박혀 있어서, 표지를 커버로 깔면 그 위의 진짜 제목과 겹쳤다. 그래서 커버는 **글자(카테고리·제목·날짜) + 무채색 배경**만으로 만들고, 대표 이미지는 **글 목록 카드와 공유 미리보기(OG)에만** 쓴다.
- **출처는 카드마다 표기한다.** 각 카드의 설명 아래에 작은 문단(`tp-source`) 하나를 두고 `© 출처명`만 적는다(예: `© 뉴스1`). 글 끝에 몰아 적지 않는다. 기준 글은 부산 상어 글(2026-09-24)이다.
- **이 글 형식은 확정이다** (2026-09-25). 앞으로 올리는 글은 모두 이 형식을 따른다.
- **영상 업로드는 Premium 요금제부터다** (부록 A.1). 콘텐츠의 약 1/3이 영상이므로 **Premium으로 결정했고 클라이언트가 결제했다** (2026-09-18, Q-31 해결).
- **이미지는 업로드 시 WordPress가 여러 크기로 자동 생성**한다. 원본은 2000×2500이고 독자 화면은 폭 360~430px다.
- **운영자는 어떤 크기 조절도 하지 않는다.** 인스타그램은 업로드 시 자동으로 축소·압축하므로 운영자는 파일 크기를 의식해본 적이 없다. 매주 수동으로 줄이게 하는 방식은 반복되다 결국 중단된다.
- **저장 공간에 한계가 있다.** 게시글 평균 58MB × 주 1회면 연 약 3GB다. Premium(13GB)은 약 4년분이다 (10장 리스크, Q-26).

---

## 5. 사용자 시나리오

### 5.1 발행 흐름

사이트 글과 뉴스레터는 **서로 다른 콘텐츠**이므로 흐름도 둘로 나뉜다 (v0.8).

> v0.7까지는 "뉴스레터 = 이번 주 사이트 글의 링크를 보내는 것"으로 설계했다. 2026-09-18 미팅에서 **뉴스레터는 별도로 작성하는 독립 콘텐츠이고 주 1~2회 보낸다**는 것이 확인되어 흐름을 다시 썼다.

**A. 사이트 글 (카드뉴스 발행에 맞춰)**

1. 운영자가 **글자 없는 사진과 스크립트(카드별 문구)**를 준비한다.
2. 작성자 또는 운영자가 **WordPress 관리자 화면에서 새 글을 만든다.** 제목(커버 문구), 카테고리, 대표 이미지(커버 사진)를 채우고, 본문은 카드마다 `사진 → 소제목 → 설명`을 반복한다 (4.3).
3. 영상이 있으면 원본 그대로 올린다. 크기를 줄이는 등의 가공은 하지 않는다.
4. **발행 전에 좋아요·공유 버튼을 끈다.** 새 글은 기본으로 켜져 있다. 2026-09-24 글 하나가 이 단계를 빠뜨려 켜진 채 발행됐다. 사이트 전체에서 끄는 방법을 찾는 중이다 (10장).
5. **발행 버튼을 누른다.** 목록 페이지 최상단에 카드가 추가되고 상세 페이지가 생긴다. 글 본문 끝에는 카카오톡 공유 버튼이 자동으로 붙는다 (F-15).
6. 이후 독자가 그 글을 볼 때마다 조회수가 집계된다.

> 운영 인계안 (2026-09-25 제안, 클라이언트 확인 전 — Q-36): 운영자가 이미 쓰는 **ChatGPT 유료 플랜에 WordPress.com 공식 앱(MCP)을 연결**하면, 사진을 미디어 라이브러리에 올린 뒤 인스타그램 본문을 GPT에 주는 것만으로 2~5단계를 GPT가 대신할 수 있다. **사진 업로드는 사람이 한다** — WordPress.com의 ChatGPT 안내에는 채팅에 첨부한 사진을 사이트에 올리는 기능이 없다(부록 A.1). GPT가 규칙대로 쓰게 하려면 **글 형식 규칙을 담은 지침 문서**를 작성자가 만들어 넘겨야 한다.

**B. 뉴스레터 (매주 금요일)**

1. 운영자가 **스티비에서 뉴스레터를 새로 쓴다.** 필요하면 사이트 글 링크를 넣을 수 있지만, 뉴스레터 자체는 사이트 글을 옮긴 것이 아니다.
2. **스티비에서 발송한다.** 대상은 스티비 주소록의 구독자 전체.
3. 구독자가 이메일을 받는다.

> ✍️ 뉴스레터에 **무엇을 쓰는지(구성·분량), 누가 쓰는지, 지난 호를 사이트에 보관할지**는 아직 정해지지 않았다 (Q-32).

### 5.2 독자 흐름

1. 독자가 인스타그램에서 카드뉴스를 보고 **프로필의 링크를 누른다.** (또는 검색으로, 또는 공유받은 링크로 들어온다)
2. **글 목록 페이지**가 열린다. 모바일 화면에 카드가 세로로 쌓여 있다.
3. 관심 있는 카드를 눌러 **상세 페이지**로 들어가 글을 읽는다.
4. 더 보고 싶으면 **카테고리 필터**로 같은 갈래의 글을 훑거나, **검색**으로 특정 주제를 찾는다.
5. 마음에 들면 **구독 폼에 이메일 주소를 입력**하고 수신동의에 체크한다.
6. 입력한 주소가 **스티비 주소록에 추가된다.**
7. 다음 발송분부터 뉴스레터를 받는다 (매주 금요일).

> 구독 폼 위치 (2026-09-17, 레퍼런스 비교 후 1차 결정 — 실제 화면을 보며 조정): **상단 메뉴의 구독 버튼 → 구독 페이지**, **글 상세 본문 아래**, **홈 하단 섹션.** 고정 배너는 모바일 읽기를 방해해 제외.
>
> 화면 목록 (1차): 홈(글 목록) / 카테고리 페이지 / 글 상세 / 검색 결과 / 구독 / 소개 / 개인정보처리방침 / 404. 광고 문의 경로는 보류.
>
> **실제 구성 (2026-09-25 확인):** 상단 메뉴는 `제보 / 소개 / 광고·협업 문의`. 구독은 별도 페이지 없이 **홈과 글 하단의 구독 칸**(`#newsletter`)으로 모았다. 페이지는 소개 / 제보 / 광고·협업 문의 / 개인정보처리방침 4개. 푸터에 인스타그램 아이콘과 `소개 · 광고·협업 문의 · 개인정보처리방침`. 홈에는 최신 제보 목록과 속보 띠가 있다.
>
> **글 끝에서 독자가 할 수 있는 것:** 카카오톡 공유(F-15) → 댓글 → 구독 칸 순서로 이어진다.

---

## 6. 기능 요구사항

> v0.7에서 번호는 유지하고 구현 방식만 바꿨다. **F-05·F-06은 WordPress 전환으로 삭제**했다. 번호를 당기지 않은 이유는 문서 곳곳의 참조가 깨지지 않게 하기 위해서다.

| 번호 | 우선순위 | 기능 | 구현 방식 | 완료 조건 |
|---|---|---|---|---|
| F-01 | 필수 | 글 목록 페이지 | 테마 | 발행된 글이 모두 최신순으로 나오고, 각 카드에서 상세 페이지로 이동된다 |
| F-02 | 필수 | 글 상세 페이지 | 테마 | 제목·카테고리·발행일·본문·미디어가 모두 나온다. **영상이 재생된다.** 목록으로 돌아갈 수 있다 |
| F-03 | 필수 | 카테고리 필터 | WordPress 기본 기능 | 관리자 화면에서 카테고리를 추가·변경해도 코드 수정 없이 반영된다 |
| F-04 | 필수 | 사이트 내 검색 | WordPress 기본 기능 (Q-12) | 제목·본문에 있는 키워드로 의도한 글이 검색된다. **이미지 속 글자는 검색되지 않는다** (7.2) |
| ~~F-05~~ | — | ~~노션 연동~~ | **삭제** (v0.7) | — |
| ~~F-06~~ | — | ~~구글 드라이브 연동~~ | **삭제** (v0.7) | — |
| F-07 | 필수 | 이미지 최적화 | WordPress 기본 기능 (자동 크기 생성) | 모바일에서 원본 크기가 아닌 축소본이 내려온다 |
| F-08 | 필수 | 뉴스레터 작성·발송 | **스티비에서 수동** | 운영자가 5.1-B를 혼자 할 수 있다 |
| F-09 | 필수 | 이메일 구독 폼 | 스티비 구독 폼 코드를 페이지에 삽입 | 주소를 입력하면 스티비 주소록에 나타난다. 수신동의 체크 없이는 제출되지 않는다 |
| F-10 | 필수 | 조회수 집계 | 플러그인 (Q-11) | 운영자가 관리자 화면에서 글별 조회수를 볼 수 있다. 봇 방문이 과도하게 반영되지 않는다 |
| F-11 | 필수 | 모바일 반응형 | 테마 | 7.1의 기준 화면 크기에서 가로 스크롤 없이 모든 요소가 보인다 |
| F-12 | 필수 | SEO 메타 태그 및 OG 태그 | 플러그인 | 링크를 공유했을 때 제목·설명·이미지가 나온다. 검색엔진에 색인된다 |
| F-13 | 필수 | 개인정보처리방침 / 수신동의 문구 | 페이지 작성 + 스티비 폼 | 구독 폼에서 처리방침으로 이동할 수 있고, 수신동의가 별도 항목으로 분리되어 있다 |
| F-14 | 선택 | ~~이메일 "웹에서 보기" 링크~~ *재정의 필요* | — | v0.7의 전제(메일 = 사이트 글 링크)가 사라졌다. 지난 뉴스레터를 웹에서 보여줄지는 Q-32에서 정한다 |
| F-15 | 필수 | 카카오톡 공유 버튼 | 자체 플러그인 `TP Kakao Share` (저장소 `plugins/tp-kakao-share/`). Kakao JavaScript SDK의 `sendScrap` | 글 끝 버튼을 누르면 카카오톡 친구 선택 창이 뜨고, 제목·이미지 카드가 전송된다. SDK를 불러오지 못하면 버튼이 보이지 않는다 |
| F-16 | 필수 | 글 댓글 | WordPress.com 기본 댓글 | 비회원이 이름·이메일로 댓글을 달 수 있다 |
| F-17 | 필수 | 제보 게시판 | KBoard + 자체 스킨 플러그인 `TP KBoard Skin` | 비회원이 제보를 쓸 수 있다. 작성자 이메일은 운영자만 본다. 운영자 글에는 운영자 표시가 붙는다. 홈에 최신 제보가 보인다 |
| F-18 | 필수 | 영상 첫 장면 미리보기 | 자체 플러그인 `TP Video Preview` (저장소 `plugins/tp-video-preview/`). 영상 블록을 화면에 내보낼 때 주소 끝에 `#t=0.001`을 붙인다 | 재생 전에도 영상 자리에 첫 장면이 보인다. 저장된 글은 바꾸지 않는다 |

> v0.7에서 삭제했던 **카카오톡 공유는 v0.9에서 다시 넣었다.** 2026-09-16에 "공유 미리보기"를 계획에서 뺐지만, 운영 중에 카카오톡 공유 버튼을 넣고 싶다는 요구가 나왔다. 버튼 방식은 세 가지를 비교했다: 카카오 공식 SDK(한 번에 카톡으로) / 폰 기본 공유창 / 링크 복사. 한국 독자에게 가장 자연스러운 첫 번째를 골랐다.

**구현 현황 (2026-09-25)**

| 상태 | 기능 |
|---|---|
| 완료 | F-01, F-02(영상 첫 장면은 F-18로 해결), F-03(카테고리 5개), F-04(글만 검색, 2글자 이상), F-07, F-09, F-11(iOS 확인 포함), F-12(카카오톡 실제 카드 확인), F-13, F-15(폰 확인. PC 동작은 미확인), F-16, F-17, F-18 |
| 남음 | F-08(첫 호 발송 전), **F-10(조회수, Q-11)**, F-14(재정의 대기) |

---

## 7. 비기능 요구사항

### 7.1 모바일 / 반응형

- **모바일 우선으로 설계한다.** 유입의 대부분이 인스타그램이므로 모바일이 기본 화면이고 데스크톱이 확장이다.
- 가로 **360px 화면까지** 가로 스크롤 없이 정상 표시된다.
- 카드뉴스 이미지는 세로로 긴 경우가 많으므로, 화면을 넘지 않도록 비율을 유지하며 축소된다.
- 터치 대상(버튼, 링크)은 손가락으로 누를 수 있는 크기를 확보한다.
- **테마를 고를 때 위 조건을 기준으로 삼는다.**
- **iOS는 실제 기기로 확인한다.** 아이폰의 브라우저는 크롬이라도 사파리와 같은 엔진(WebKit)을 쓴다. 2026-09-25 구독 칸이 iOS에서만 화면 밖으로 넘쳤는데, 데스크톱 크롬으로 흉내 낸 모바일 화면에서는 재현되지 않았다. 입력칸이 기본 폭을 고집하는 WebKit 동작이 원인이었다.

### 7.2 SEO 및 공유 미리보기

- 각 글마다 고유한 제목과 설명 메타 태그를 갖는다.
- **OG 태그**(제목, 설명, 대표 이미지)를 제공한다. 링크를 붙였을 때 미리보기가 뜬다.
  - 2026-09-25 **카카오톡에서 실제로 확인했다.** 링크만 붙여도 카드가 뜨고, 공유 버튼(F-15)으로 보내면 큰 이미지 + `트렌드포털에서 확인` 버튼 + 하단 앱 이름(트렌드포털)이 붙은 카드가 간다. HTML에 태그가 있는지만 보지 않고 **실제 카톡 전송으로 확인**한 이유는, 예전에 태그가 정상인데도 카카오 수집기가 차단돼 카드가 안 뜬 적이 있기 때문이다.
- 대표 이미지는 해당 글의 카드뉴스 첫 장을 사용한다. 영상만으로 된 글도 대표 이미지는 정지 이미지로 둔다.
- `sitemap.xml`과 `robots.txt`를 제공한다. 2026-09-18 DNS를 WordPress.com으로 옮겨서 **Cloudflare의 봇 설정은 더 이상 적용되지 않는다** (Q-29).
- **이미지 속 글자는 검색엔진이 내용으로 읽지 못한다.** 구글 공식 가이드도 중요한 글자는 이미지가 아니라 HTML 텍스트로 두고 이미지에는 alt 텍스트를 달라고 권한다(부록 A.1). 사이트 글을 "글자 없는 사진 + 텍스트"로 만들기로 하면서(4.3) **본문 텍스트 문제는 해결됐다.** alt 텍스트를 어디까지 요구할지는 Q-30에 남아 있다.

### 7.3 성능

- 모바일 네트워크에서 **목록 페이지의 첫 화면이 실용적인 시간 안에 뜬다.** 구체적 기준은 배포 후 실측해서 정한다.
- **이미지는 축소본을 내보낸다** (F-07). 원본을 그대로 내보내면 글 하나에 40MB 이상을 내려받게 된다.
- 이미지와 영상은 **화면에 들어올 때 불러온다.** 한 글에 미디어가 9개 안팎이므로 한 번에 모두 받으면 느려진다.
- **영상은 재인코딩하지 않는다.** 원본 중 45MB짜리도 있어 모바일에서 부담이 될 수 있다. 배포 후 실측한 뒤 대응 여부를 판단한다 (Q-22).

> ✍️ 성능은 숫자를 먼저 정하고 맞추기보다, 실제로 배포한 뒤 측정해서 기준을 잡는 편이 낫습니다. ⑧ 단계에서 실측 후 이 칸을 갱신하세요.

### 7.4 법적 요건

> 구체적 요건은 로드맵 ⑦ 단계에서 법령 원문으로 확인할 예정이다 (Q-07).

- **개인정보처리방침 게시** — 구독자 이메일을 수집하므로
- **수신동의 절차** — 구독 시점에 동의를 받고 그 기록을 남긴다. 동의는 다른 약관과 분리된 별도 항목이어야 한다
- **수신거부 방법 제공** — 스티비 기능을 사용한다
- **광고성 정보 요건** — 광고성 정보에 해당하면 제목 앞 `(광고)` 표기, 무료 수신거부 방법 명시, 2년마다 수신동의 여부 확인이 필요하다고 안내하는 자료가 있다 (부록 A.2, 원문 미확인)
- **발신 도메인 인증 (SPF / DKIM)** — 법적 요건은 아니지만, 없으면 메일이 스팸함으로 분류된다. 스티비를 붙일 때 설정한다. **SPF 레코드는 도메인에 한 줄만 둘 수 있으므로** 서비스를 추가할 때마다 기존 줄에 `include:`를 합친다
- **진행 상황 (v0.9):** 개인정보처리방침 페이지를 만들고 **두 번 개정**했다(뉴스레터 구독 이메일, 제보 게시판 이메일 수집 추가). 구독 칸에는 `(필수) 개인정보 수집 및 이용`과 `(필수) 광고성 정보 수신` 동의를 **체크박스 두 개로 분리**했다. `contact@`의 SPF·DKIM·DMARC는 Google Workspace로 설정을 마쳤다 (부록 B)
- ✍️ **아직 없는 것:** 이용약관 페이지(Q-38), 푸터의 사업자 정보(Q-37)

### 7.5 유지보수

- 운영자가 **코드를 만지지 않고** 콘텐츠를 추가·수정·삭제할 수 있다.
- 카테고리가 바뀌어도 **코드 수정이 필요 없다.**
- API 키와 같은 비밀값은 저장소에 커밋되지 않는다.
  - 예외: 카카오 **JavaScript 키**는 원래 웹페이지에 공개되는 키이고, 등록한 도메인(`trendportal.kr`)에서만 작동해서 플러그인 코드에 들어 있다. **REST API 키는 이와 달리 절대 노출하면 안 된다.**
- **사이트 모양 코드가 두 곳에 나뉘어 있다.** 자체 플러그인(`TP Kakao Share`, `TP KBoard Skin`)은 저장소에 두고, 버튼·레이아웃 CSS는 WordPress의 **추가 CSS**에 있다. 추가 CSS는 저장소 밖이라 **백업이 없다** (10장).
- **작성자 계정에 묶인 것은 인계 때 넘겨야 한다:** Kakao Developers 앱(현재 작성자 계정이 Owner → 멤버 추가 후 Owner 이전), 도메인 등록 명의(가비아, 작성자).

---

## 8. 기술 스택 및 선택 근거

| 항목 | 선택 | 근거 / 기각한 대안 |
|---|---|---|
| CMS / 호스팅 | **WordPress.com Premium** (2026-09-18 결제). 테마는 **Spiel**을 기반으로 수정한다 (2026-09-18 합의) | ✍️ *작성자가 직접 작성.* 참고할 사실: 부록 A.1(WordPress.com 요금제·도메인·플러그인 조건), 부록 E(국내외 매체 CMS 조사)<br><br>**기각 — Next.js + 노션 + 구글 드라이브 (v0.6까지의 구조)**: ✍️ *작성자가 직접 작성*<br>**기각 — Ghost**: ✍️ *작성자가 직접 작성.* 참고할 사실: 부록 A.1(Ghost 요금제별 업로드 한도)<br>**기각 — WordPress.com 무료 플랜**: 자체 도메인 연결 불가, 저장 공간 1GB(게시글 약 17건분), 코드 삽입 불가로 스티비 구독 폼을 붙일 수 없다 (부록 A.1) |
| 이메일 발송 | 스티비 스탠다드 (수동 발송) | 무료 플랜은 **월 2회 발송 한도**인데 뉴스레터는 매주(월 4~5회)다. 구독 폼과 구독자 API는 스탠다드부터 쓸 수 있지만, **자동 발송에 필요한 이메일 API는 프로 요금제부터**다 (부록 A.1). 수동 발송을 택한 이유: ✍️ *작성자가 직접 작성*<br><br>**기각 — Kit / beehiiv / WordPress 자체 뉴스레터**: 해외 서비스라 한국 정보통신망법 요건(수신동의 기록, 광고 표기)을 직접 설계해야 한다. 월 8,900원은 **그 요건을 플랫폼이 대신 지켜주는 값**이다. WordPress 자체 뉴스레터는 **글 발행이 곧 메일 발송**이라, 뉴스레터를 따로 쓰는 운영 방식과도 맞지 않는다 (v0.8).<br>**기각 — Gmail·Workspace로 직접 발송**: 구독자 명단, 수신거부 처리, 법적 요건을 모두 손으로 챙겨야 한다. |
| DNS | **WordPress.com** (요금제 포함, 2026-09-18 네임서버 이전) | ✍️ *작성자가 직접 작성.* 참고할 사실: 2026-09-17까지는 Cloudflare(무료)였다. 옮기면서 Cloudflare Email Routing(`contact@` 수신 포워딩)과 AI 봇 제어 설정이 효과를 잃었다 (부록 B, Q-29) |
| 업무 메일 | **Google Workspace** (`contact@trendportal.kr`, 2026-09-22) | ✍️ *작성자가 직접 작성.* 참고할 사실: 후보는 WordPress.com Professional Email(9장), 원했던 것은 받는 사람 화면의 프로필 사진(로고)과 "도메인: gmail.com" 표시 제거. SPF·DKIM·DMARC 설정 완료 (Q-28 해결) |
| 커뮤니티 (제보 게시판) | **KBoard** + 자체 스킨 플러그인 `TP KBoard Skin` | ✍️ *작성자가 직접 작성.* 참고할 사실: KBoard 댓글은 KBoard 글에만 붙어 일반 글 댓글은 WordPress.com 기본 댓글을 쓴다 (F-16). 스킨은 기본 스킨을 복사해 최신 글 썸네일을 넣고 추천(좋아요) 기능을 뺐다. **스킨 플러그인은 아직 저장소 밖에 있다** |
| 공유 | **자체 플러그인 `TP Kakao Share`** — Kakao JavaScript SDK 2.8.3(버전 고정 + integrity 검사), `Kakao.Share.sendScrap` | 스크랩 방식은 카카오가 페이지를 직접 읽어 카드를 만들어서, 글마다 넘길 값이 없다. 버튼은 템플릿의 숏코드 `[tp_kakao_share]` 자리(글 본문 끝)에 나온다. **기각 — 폰 기본 공유창(Web Share API)**: 카톡까지 한 번 더 눌러야 하고 PC 대체가 필요하다. **기각 — 링크 복사만**: 독자가 카톡을 직접 열어 붙여넣어야 한다. 버튼 모양은 레퍼런스 8곳 조사 후 무채색 알약형 + 카카오 공식 노란 아이콘으로 정했다(공식 공유 아이콘은 노란색만 제공된다, 부록 A.1) |
| 조회수 집계 | *미정 — 플러그인 (Q-11)* | |
| 검색 | WordPress 기본 검색. 글만 검색하고 2글자 이상에서 동작하도록 맞췄다 (Q-12) | |

> v0.6까지 이 표에 있던 Next.js, TypeScript, Tailwind CSS, shadcn/ui, 21st.dev, Google Drive, Notion API, 호스팅(Netlify/Cloudflare) 항목은 WordPress 전환으로 삭제했다. 근거는 **커밋 `6959fac`의 v0.5 8장에 남아 있다** — 단, Google Drive는 v0.6에서 추가된 항목이라 그 근거는 남아 있지 않다.

---

## 9. 비용

| 항목 | 금액 | 누가 부담 | 상태 |
|---|---|---|---|
| 도메인 (`trendportal.kr`) | **16,500원 / 1년** (가비아). 만기 2027-09-16. 갱신가는 미확인 | 작성자 | **결제 완료** (2026-09-16) |
| DNS | 0원 (WordPress.com 요금제 포함) | — | 사용 중 (2026-09-18 Cloudflare에서 이전) |
| WordPress.com Premium | 월 $8 (연 결제) / 월 $18 (월 결제) | 클라이언트 | **결제 완료** (2026-09-18). 연 결제인지 월 결제인지는 미확인 |
| 이메일 발송 (스티비 스탠다드) | 월 8,900원~ / 연 96,000원 (구독자 0~500명 구간, VAT 포함). 구독자가 늘면 요금 구간이 올라간다 | 클라이언트 | 실제 발송 시작 시점 |
| 업무 메일 (`contact@`) | **Google Workspace** — **월 결제**, 사용자 1명당 월 $8.40 (연 약정이면 월 $7). *기각한 후보: WordPress.com Professional Email, 메일함당 연 $35 (부록 A.2)* | 클라이언트 | **사용 중** (2026-09-22, Q-28 해결) |
| 카카오톡 공유 (Kakao Developers) | 0원 | — | 사용 중 (2026-09-25). 앱 소유자는 작성자 계정, 클라이언트에게 이전 예정 (7.5) |

---

## 10. 제약 및 리스크

| 리스크 | 영향 | 대응 방안 |
|---|---|---|
| 운영자가 만들어야 할 콘텐츠가 늘었다 — 카드뉴스(인스타용) + 글자 없는 사진·스크립트(사이트용) + 뉴스레터 매주 | 매주 부담이 커지면 사이트 글이나 뉴스레터가 끊길 수 있다 | 사이트 글은 작성자가 대신 쓸 수 있게 했다 (3.1) |
| 저장 공간이 요금제로 묶여 있다 — Premium 13GB | 실측 평균 58MB/건(부록 D) 기준 주 1회 발행이면 약 229건(약 4년)이면 찬다. 과거 게시물을 옮기면 그만큼 앞당겨진다 (Q-26). **WordPress가 생성하는 썸네일 용량은 계산에 넣지 않았다** | 이미지 최적화(F-07)로 늘릴 수 있다. 한계에 가까워지면 상위 요금제나 자체 호스팅으로 이전 |
| 영상이 콘텐츠의 약 1/3이고 최대 45MB다 | 모바일 독자에게 부담이 크다. 영상만으로 이루어진 게시글도 있어 생략할 수 없다 | |
| 카드뉴스의 핵심 내용이 이미지 안에 있다 | 검색 유입(2장 목표)과 사이트 내 검색에 불리하다 (7.2) | |
| 스티비 무료 플랜은 월 2회 발송 한도가 있다 | 뉴스레터 매주(월 4~5회)와 맞지 않아 유료 플랜이 필요하다 | |
| 스티비 구독 폼 코드는 스티비 쪽 수정이 자동 반영되지 않는다 | 스티비에서 폼을 고치면 코드를 다시 내보내 사이트에 붙여야 한다 | |
| 카테고리가 바뀔 수 있다 (v0.9에 `뉴스`가 추가돼 5개) | WordPress 카테고리로 관리하므로 코드 수정은 필요 없다 | `뉴스`의 정의는 Q-34 |
| 새 글마다 좋아요·공유 버튼이 기본으로 켜진다 (WordPress.com/Jetpack) | 끄는 걸 잊으면 우리가 뺀 기능(4.2)이 글에 나타난다. 2026-09-24 실제로 한 번 누락됐다 | 발행 절차에 "끄기"를 넣었다 (5.1). 사이트 전체에서 끄는 방법을 찾는 중 |
| 버튼·레이아웃 CSS가 WordPress 추가 CSS에만 있다 | 저장소 밖이라 백업과 변경 기록이 없다. 누가 통째로 덮어쓰면 되돌릴 수 없다 | 추가 CSS를 저장소에 사본으로 두는 방법을 검토한다 |
| iOS(WebKit)에서만 생기는 화면 문제가 있다 | 데스크톱 크롬으로는 재현되지 않아 놓치기 쉽다 (7.1) | 모양을 바꾸면 아이폰 실기기로 확인한다 |
| 모바일에서 영상이 재생 전까지 흰 칸으로 보인다 (iOS·모바일 크롬) | 독자가 영상인지 모르고 지나칠 수 있다 | **해결 (2026-09-25):** 자체 플러그인 `TP Video Preview`가 영상 주소 끝에 `#t=0.001`을 붙여 첫 장면을 보여 준다 (F-18). 영상마다 대표 이미지를 만들 필요가 없다 |
| 인계 대상이 작성자 계정에 묶여 있다 — Kakao Developers 앱, 도메인 등록 명의 | 작성자가 연락되지 않으면 키 재발급·도메인 갱신을 할 수 없다. 카카오 데브톡에는 이런 소유권 이전 요청 글이 실제로 여러 개 있다 | 앱은 멤버 추가 → Owner 이전으로 넘긴다 (7.5) |
| 조회수에 검색엔진 크롤러(봇) 방문이 섞인다 | 숫자가 실제 독자 수를 반영하지 못해 2.1의 "조회수 1,000" 기준이 흔들린다. 플러그인 선택 시 확인한다 | |
| 한국어 검색은 띄어쓰기와 어미 변화 때문에 단순 문자열 일치로는 원하는 결과가 나오지 않는다 | "트렌드 포털"과 "트렌드포털"이 서로 검색되지 않는 등 독자가 글을 못 찾는다 | |

---

## 11. 미확정 사항

| 번호 | 질문 | 답해야 할 사람 | 상태 |
|---|---|---|---|
| Q-07 | 한국 정보통신망법상 광고성 이메일 요건을 어디까지 지켜야 하는가. 이 뉴스레터가 광고성 정보에 해당하는가 | 작성자 (원문 확인) | 대기 중. 뉴스레터가 독립 콘텐츠가 되면서(v0.8) 광고 내용을 담는지가 직접 걸린다 |
| Q-11 | 조회수를 어떤 플러그인으로 집계할 것인가 (봇 제외 여부 포함) | 작성자 | 구현 단계에서 결정 |
| Q-12 | WordPress 기본 검색으로 한국어 검색이 충분한가 | 작성자 | 기본 검색을 글만·2글자 이상으로 맞춰 쓰는 중. 글이 쌓인 뒤 한국어 검색 품질을 다시 본다 |
| Q-13 | 이메일 구독자 수 목표치를 얼마로 잡을 것인가 | 클라이언트 + 작성자 | **보류** — 클라이언트가 우선순위를 낮게 둠 (2026-09-18) |
| Q-16 | 2.1 목표치의 비교 기준과 측정 기간을 무엇으로 할 것인가 (조회수 1,000은 어느 기간 기준인가, 광고 10% 증가는 무엇 대비인가) | 클라이언트 + 작성자 | **보류** — 2026-09-18 답: 광고 문의 10%는 현재 광고 건수 대비, 한 달 기준 정도. 조회수 기간은 미정. 클라이언트가 우선순위를 낮게 둠 |
| Q-22 | 영상을 재인코딩할 것인가 | 작성자 | ⑧에서 실측 후 판단 |
| Q-26 | 과거 인스타그램 게시물을 사이트로 몇 건 옮길 것인가 (저장 공간에 직결) | 클라이언트 | 사이트 글이 2편뿐이라 옮기는 방향으로 기울었다. 무엇을 옮길지는 Q-35의 시리즈 단위로 정한다. 원본은 클라이언트가 인스타그램 공식 "내 정보 내보내기"로 받는 방법을 제안할 예정 (부록 A.1) |
| Q-29 | AI 검색·에이전트·학습 봇의 사이트 수집을 허용할 것인가 | 클라이언트 | 대기 중. DNS 이전으로 Cloudflare 설정은 효과가 없어져, WordPress.com 쪽 방법을 새로 찾아야 한다 |
| Q-30 | 운영자에게 이미지 alt 텍스트를 어디까지 요구할 것인가 | 작성자 + 클라이언트 | 대기 중. 본문 텍스트는 사이트용 글 형식(4.3)으로 해결 |
| Q-32 | 뉴스레터에 무엇을 쓰는가(구성·분량), 누가 쓰는가, 지난 호를 사이트에 보관할 것인가 (F-14). *발행 주기는 사이트 문구대로 매주 금요일로 정리했다 (3.1)* | 클라이언트 + 작성자 | 대기 중. 스티비 인수인계 때 템플릿과 함께 정한다. 작성은 "GPT로 초안 → 스티비 편집기에 붙여 직접 발송"을 제안할 예정 |
| Q-34 | `뉴스` 카테고리에는 어떤 글을 넣는가 (브랜드·트렌드와 어떻게 나누는가) | 클라이언트 | 대기 중 (v0.9 신설) |
| Q-35 | **시리즈 탭**을 만들 것인가 — `주간 총정리`(블로그·인스타의 "이번 주 마케팅 이슈 한 번에 총정리", 매주)와 `TP 1,000`(인스타 하이라이트, 저장 1,000회 넘은 글). 카테고리가 아니라 **태그로 묶고 상단 메뉴에 링크**한다. 탭마다 글이 3편 이상 모이면 메뉴에 연다 | 클라이언트 | **클라이언트 확인 전 착수하지 않음** (v0.9 신설). 작성자 쪽 안: 탭 이름·주소는 `주간 총정리`(`weekly`)·`TP 1,000`(`tp1000`) |
| Q-36 | 사이트 글 올리기를 **운영자의 ChatGPT + WordPress.com 앱(MCP)**으로 넘길 것인가 (5.1 운영 인계안). 넘긴다면 작성자가 글 형식 지침 문서를 만든다 | 클라이언트 + 작성자 | 제안 예정 (v0.9 신설) |
| Q-37 | 사업자 등록을 했는가 — 푸터에 사업자 정보를 넣을지가 여기 달렸다 | 클라이언트 | 대기 중 (v0.9 신설). 없는 정보는 적지 않는다 |
| Q-38 | 이용약관 페이지가 필요한가 — 제보 게시판·댓글로 독자가 글을 쓰게 됐다 | 작성자 + 클라이언트 | 대기 중 (v0.9 신설) |
| Q-39 | 스티비 API로 뉴스레터 작업을 자동화할 가치가 있는가 (이메일 API는 프로 요금제, 공식 MCP 없음, Zapier 같은 대안) | 작성자 | 조사 예정 (v0.9 신설). 지금은 연동하지 않는다 (4.2) |

**해결됨**

- Q-28 (업무 메일) — **Google Workspace** (2026-09-22). 수신·발신·SPF·DKIM·DMARC 설정 완료
- Q-33 (댓글·커뮤니티) — **넣었다.** 글 댓글은 WordPress.com 기본 댓글(F-16), 커뮤니티는 KBoard 제보 게시판(F-17). 둘 다 **비회원(이름·이메일)** 방식이고 로그인·회원 기능은 넣지 않았다 (v0.9)
- Q-10 (문의 주소) — **`contact@trendportal.kr`로 확정** (2026-09-18)
- Q-17 (스티비 비용) — 클라이언트 부담. 비용이 생기면 클라이언트가 낸다 (2026-09-18)
- Q-27 (WordPress.com 비용) — 클라이언트가 Premium을 결제했다 (2026-09-18)
- Q-31 (Personal vs Premium) — **Premium** (2026-09-18)

- Q-02 (도메인 비용 부담) — 작성자가 부담. 2026-09-16 구매 완료
- Q-14 (방문자 측정 수단) — 조회수 집계를 범위에 다시 포함하여 해소
- Q-15 (인스타그램 링크 공개) — 2026-09-16 포함해도 된다는 확인을 받음
- Q-01 / Q-09 / Q-20 (도메인) — **`trendportal.kr`로 확정.** `trendportal.com`과 `trend-portal.com`은 이미 등록되어 있어(각각 2009년, 2008년 등록) 사용할 수 없다. 독자가 한국어권이므로 `.io`·`.net`보다 `.kr`이 맞는다고 판단했다
- Q-06 (카테고리) — 브랜드 / 트렌드 / 광고 / 인터뷰 넷으로 확정. WordPress 카테고리로 관리한다. *v0.9에서 `뉴스`가 추가돼 다섯 개가 됐다 (Q-34)*
- 발송 주기 — **카드뉴스는 매일, 뉴스레터는 별도 콘텐츠로 매주 금요일** (v0.9, 사이트 문구 기준. v0.8에는 "카드뉴스 매주 금요일, 뉴스레터 주 1~2회"였다)
- Q-03 (호스팅) — WordPress.com이 호스팅을 포함하므로 해소 (v0.7)
- Q-04 (노션 자동 발송 구조 유지 여부) — 유지하지 않음. 스티비 수동 발송 (v0.7)
- Q-05 (스티비 무료 플랜 API) — 무료 플랜은 API 불가. 구독자 API는 스탠다드, 이메일 API는 프로부터 (v0.7, 부록 A.1)

**폐기됨 (v0.7, 노션·드라이브 구조 삭제로 질문 자체가 사라짐)**

- Q-08 (노션 발송 트리거), Q-18 (노션 데이터베이스 구성), Q-19 (노션 연동 권한), Q-21 (드라이브→노션 유료 전환 조건), Q-23 (드라이브 접근 권한), Q-24 (드라이브 미디어 표시 순서), Q-25 (노션 유료 이미지 한도)

---

## 12. 단계별 계획

> ✍️ WordPress 전환으로 ③~⑦의 내용이 "개발"에서 "설정·테마·플러그인 선택"으로 바뀌었다. **단계를 다시 나누고 "완료 조건" 칸을 채우세요.** 날짜나 소요 기간은 쓰지 않습니다.

| 단계 | 내용 | 완료 조건 |
|---|---|---|
| ① | 기획 마무리 (PRD 작성) | |
| ② | 디자인 시안 | |
| ③ | 프로젝트 셋업 | |
| ④ | 리스트 / 상세 페이지 구현 | |
| ⑤ | ~~노션 API 연동~~ *재정의 필요* | |
| ⑥ | 반응형 / 모바일 최적화 | |
| ⑦ | SEO / 법적 요소 | |
| ⑧ | QA / 배포 | |
| ⑨ | 포트폴리오 문서화 | |

---

## 부록 A. 확인된 사실

### A.1 공식 문서 원문으로 확인한 것

- **스티비 스타터(무료)**: 구독자 최대 500명, 발송 횟수 월 2회. API 사용 불가. — <https://help.stibee.com/pricing/understanding/type>
- **스티비 스탠다드**: 월 8,900원~ (구독자 0~500명 구간 / 연 96,000원, VAT 포함), 발송 횟수 무제한. 유료 플랜의 구독자 수 표기는 상한이 아니라 **가격 구간**이다. — 같은 출처
- **스티비 프로**: 월 29,000원~ (구독자 0~500명 구간). — 같은 출처
- **스티비 API 요금제별 범위**: 구독자 API는 스탠다드·프로·엔터프라이즈, **이메일·그룹 API는 프로·엔터프라이즈**, 주소록·발신자 주소 API는 엔터프라이즈만. — <https://help.stibee.com/api-webhook/api>
  - v0.6까지 이 문서는 "스탠다드로 자동 발송(이메일 API)이 가능하다"고 적었는데 **틀린 서술이었다.** v0.7에서 바로잡았다
- **스티비 구독 폼**: HTML 코드로 내보내 외부 사이트에 삽입할 수 있다. 내보낸 코드에는 스티비 쪽 수정이 자동 반영되지 않으므로, 폼을 고치면 다시 내보내야 한다. — <https://help.stibee.com/list/gather-subscribers/form>
- **WordPress.com 도메인**: 자체 도메인 연결은 유료 플랜에서만 가능하다. — <https://wordpress.com/support/domains/>
- **WordPress.com 플러그인**: Personal, Premium, Business, Commerce 등 모든 유료 플랜에서 설치 가능하다. — <https://wordpress.com/support/plugins/>
- **WordPress.com 코드 삽입**: 자바스크립트·iframe·HTML 폼은 플러그인이 가능한 플랜에서 호스팅 기능을 활성화해야 쓸 수 있다. — <https://wordpress.com/support/code/>
- **WordPress.com 요금제** (2026-09-18 재확인): Free 1GB / Personal 6GB, 월 $4(연 결제)·$9(월 결제) / Premium 13GB, 월 $8(연 결제)·$18(월 결제) / Business 50GB, 월 $25(연 결제)·$40(월 결제). 유료 플랜은 방문자에게 광고를 노출하지 않는다. — <https://wordpress.com/pricing/>
- **WordPress.com 업로드**: 파일 하나당 최대 2GB까지 안정적으로 업로드된다. — <https://wordpress.com/support/accepted-filetypes/>
- **WordPress.com 영상 업로드**: Free·Personal 불가. **Premium부터 "Upload videos"** — 다만 영상도 요금제의 13GB 공용 저장 공간을 쓴다. **VideoPress(전용 저장소 250GB)는 Business·Commerce부터**이며, VideoPress 도움말도 *"available on sites with the WordPress.com Business and Commerce plans"*로 같게 적는다. — <https://wordpress.com/pricing/>, <https://wordpress.com/support/videopress/>
  - 2026-09-17에 이 문서는 "Premium에 VideoPress 250GB가 포함되고 공식 문서끼리 어긋난다"고 적었는데 **둘 다 틀렸다.** 요금제 페이지를 잘못 읽은 것이고, 두 문서는 어긋나지 않는다. 2026-09-18에 재확인해 바로잡았다
- **Ghost(Pro) 파일 업로드 한도**: 체험·Starter 5MB, Publisher 100MB, Business 250MB. Starter는 커스텀 테마·자체 도메인 발신 불가. Publisher 월 $29(연 결제). — <https://ghost.org/help/media-file-size-limits/>, <https://ghost.org/pricing/>
- **구글 이미지 SEO 가이드**: 제목·메뉴 같은 중요한 글자를 이미지 안에 넣지 말고 HTML 텍스트로 두며, 이미지에는 alt 텍스트를 제공하라고 권한다. — <https://developers.google.com/search/docs/appearance/google-images>
- **Google Workspace**: 14일 무료 체험. Flexible 요금제는 언제든 위약금 없이 해지 가능. 사용자 1명당 별칭 주소 30개까지 추가 비용 없음. 유료 계정 발송 한도 하루 2,000통(체험 계정 500통). — <https://knowledge.workspace.google.com/admin/getting-started/about-your-google-workspace-free-trial>, <https://support.google.com/a/answer/1247362>, <https://support.google.com/a/answer/33327>, <https://knowledge.workspace.google.com/admin/gmail/gmail-sending-limits-in-google-workspace>
- **Cloudflare Email Routing**: 규칙 하나는 받는 곳 하나(또는 Worker 하나)로만 전달한다. — <https://developers.cloudflare.com/email-routing/setup/email-routing-addresses/>
- **노션 무료 플랜의 파일 업로드 한도**: *"all files you upload must be under 5MB"*. v0.6 판단의 근거로 남겨 둔다. — <https://www.notion.com/help/images-files-and-media>
- **인스타그램은 업로드 시 자동으로 축소·압축한다.** 가로 1080px를 넘으면 1080으로 줄이고 다시 압축한다. 권장 피드 규격은 1080×1350(4:5)이다. 그래서 클라이언트는 파일 크기를 의식해본 적이 없다.
- **카카오톡 공유 (JavaScript SDK)** (2026-09-25 확인): 최신 2.8.3(2026-09-03 배포). 방식은 기본 템플릿(`sendDefault`)·커스텀 템플릿(`sendCustom`)·스크랩(`sendScrap`) 세 가지. 스크랩은 **공유할 페이지 도메인을 [앱] > [제품 링크 관리] > [웹 도메인]에 등록**해야 하고, SDK 자체는 **[플랫폼 키] > JavaScript 키 > JavaScript SDK 도메인**에 등록한 곳에서만 실행된다. 같은 페이지에서 `Kakao.init`을 두 번 부르면 에러가 난다(SDK 소스에서 확인). — <https://developers.kakao.com/docs/ko/kakaotalk-share/js-link>, <https://developers.kakao.com/docs/ko/javascript/download>
- **카카오톡 공유 아이콘**: 카카오가 공유용으로 제공하는 아이콘은 **노란색 2종뿐**이다(TALK 글자 있음/없음). 공유 버튼의 색 변경 규정은 공유 문서에 없다. 참고로 **로그인 버튼 가이드**는 "심볼의 형태, 비율, 색상은 변경할 수 없다"고 적는다. — <https://developers.kakao.com/tool/resource/talk>, <https://developers.kakao.com/docs/latest/ko/kakaologin/design-guide>
- **Kakao Developers 앱 소유권**: Owner가 다른 계정을 [앱] > [멤버]에 추가한 뒤 역할을 Owner로 바꿔 넘길 수 있다. — <https://developers.kakao.com/docs/ko/app-setting/app>
- **ChatGPT의 MCP 연결**: 개발자 모드는 Pro·Plus·Business·Enterprise·Education 계정의 웹에서 쓸 수 있고, 원격 MCP 서버의 읽기·쓰기 도구를 모두 지원한다. — <https://help.openai.com/en/articles/12584461-developer-mode-and-mcp-apps-in-chatgpt>
- **WordPress.com + ChatGPT**: ChatGPT 안의 WordPress.com 앱으로 연결한다(유료 요금제 + MCP 설정 켜기). 글 초안 작성·발행이 되고, 쓰기 작업마다 확인을 받는다. 미디어는 **이미 올린 파일의 정보 수정**만 안내돼 있고, 채팅에 첨부한 사진을 올리는 기능은 적혀 있지 않다. — <https://wordpress.com/support/mcp/connect-chatgpt/>
- **스티비 API** (2026-09-25): 이메일 생성·편집·발송, 주소록·구독자·그룹을 다룬다. 공식 MCP 언급은 없다. — <https://developers.stibee.com/>, <https://help.stibee.com/api-webhook/overview>
- **인스타그램 내 정보 내보내기**: 계정 센터 → 내 정보 및 권한 → 내 정보 내보내기에서 정보 종류·기간·형식(JSON)·미디어 품질을 골라 받을 수 있다. — <https://help.instagram.com/181231772500920/>

### A.2 아직 공식 출처로 확인하지 못한 것

> 아래는 3rd-party 요약만 본 것이다. **이 상태로 문서 본문의 근거로 쓰지 말 것.** 확인한 뒤 A.1로 옮긴다.

- Kit(구 ConvertKit) 무료 플랜 구독자 10,000명 / beehiiv 무료 플랜 2,500명, 양쪽 모두 발송 무제한
- 한국 정보통신망법상 광고성 정보 전송 시의 구체적 요건 — `(광고)` 표기, 무료 수신거부 방법 명시, 2년마다 수신동의 확인 (법령 원문은 Q-07에서 확인)
- WordPress 기본 검색의 한국어 처리 수준
- WordPress.com Professional Email — 메일함당 연 $35, 첫 3개월 무료, 30GB, IMAP/SMTP, SPF·DKIM·DMARC 자동 설정 (2026-09-18 조사 메모. 공식 요금 페이지로 다시 확인할 것)

---

## 부록 B. 이 프로젝트에 등장하는 세 종류의 이메일

이름이 비슷해서 혼동하기 쉬우므로 구분해 둔다.

| # | 무엇 | 주소 | 누가 처리 | 현재 상태 |
|---|---|---|---|---|
| 1 | 업무 메일 (문의·광고주 소통) | `contact@trendportal.kr` (확정) | **Google Workspace** (Q-28 해결) | **정상.** 2026-09-22 Workspace로 수신·발신·SPF·DKIM·DMARC·프로필 사진까지 설정했다. 사이트의 문의 페이지·소개 페이지·푸터 주소도 이 주소로 바꿨다(2026-09-23~25). *인스타그램 소개란은 클라이언트가 진행 중인 광고주 연락 때문에 아직 옛 Gmail 주소다* |
| 2 | 뉴스레터 발신인 | `news@trendportal.kr` (예정) | 스티비가 발송한다 | 스티비 발신자 인증을 마쳤다. ✍️ **어느 주소로 인증했는지 확인 필요** |
| 3 | 구독자 이메일 (독자가 입력) | 독자들의 주소 | 스티비 주소록에 저장된다 | 구독 폼 (F-09) |

도메인은 건물 주소에, 웹사이트는 정문에, 이메일은 우편함에 해당한다. 같은 주소를 쓰지만 서로 다른 DNS 레코드를 보며, 정문과 우편함을 서로 다른 회사에 맡길 수 있다.

---

## 부록 C. 노션 데이터베이스 명세 (폐기)

v0.7에서 WordPress로 전환하면서 폐기했다. 속성 목록, 슬러그 규칙, 클라이언트 주의사항은 **커밋 `6959fac`의 v0.5 부록 C에 남아 있다.** 다만 v0.6에서 추가했던 **구글 드라이브 미디어 처리 방식은 남아 있지 않다** — v0.6을 커밋하지 않은 채 v0.7로 덮어썼다.

---

## 부록 D. 실제 콘텐츠 실측 (2026-09-16)

클라이언트가 제공한 카드뉴스 **14건**을 직접 측정한 결과다. v0.6의 미디어 처리 방식과 v0.7의 저장 공간 계산(4.3)의 근거가 된다.

### D.1 구성

| 항목 | 값 |
|---|---|
| 게시글 수 | 14건 |
| 게시글당 슬라이드 | 2 ~ 14장 (평균 9장) |
| 게시글당 용량 | 17MB ~ 102MB (평균 58MB) |
| 전체 용량 | 807MB |
| 이미지 해상도 | 2000 × 2500 (4:5) — 인스타 권장 규격 1080×1350의 약 1.85배 |

### D.2 이미지와 영상의 비율

**영상이 콘텐츠의 약 1/3이다.** 14건 중 11건에 영상이 있고, 영상이 대부분인 게시글도 있다 (한 건은 9장 중 8장이 영상).

| | 개수 | 크기 (최소 / 중앙값 / 최대) |
|---|---|---|
| 이미지 (PNG) | 86개 | 0.6MB / 3.3MB / **13.0MB** |
| 영상 (MP4) | 41개 | 0.2MB / 7.1MB / **45.6MB** |

### D.3 노션 무료 5MB 한도로 거른 결과

| | 전체 | 5MB 초과 |
|---|---|---|
| 이미지 | 86개 | **29개 (34%)** |
| 영상 | 41개 | **26개 (63%)** |
| **합계** | 127개 | **55개 (43%)** |

**14개 게시글 전부에 5MB를 넘는 파일이 최소 1개씩 있다.** 같은 5MB 한도가 Ghost(Pro) Starter에도 있어, Ghost를 기각할 때도 이 표가 근거가 됐다.

---

## 부록 E. 국내외 매체 CMS 조사 (2026-09-17)

각 사이트의 홈페이지와 CMS 전용 경로(`/wp-json/` 등)를 직접 조회해 흔적을 확인했다. **통계가 아니라 작성자가 고른 약 20곳의 표본**이며, 직접 개발한 사이트는 내부 CMS를 알 수 없다.

| 도구 | 확인된 사이트 |
|---|---|
| WordPress | 디에디트, 어피티(+ 스티비), 데이즈드 코리아, 보그·얼루어·W·GQ 코리아, 뉴스페퍼민트, 하입비스트, Dezeen, The Verge(WordPress + Next.js) |
| Ghost | 404 Media, Platformer (해외 유료 뉴스레터 매체) |
| 직접 개발 (추정) | 캐릿, 뉴닉, 퍼블리, 아이즈매거진, 엘르 코리아 |
| 기타 | 부딩(Wix), 매거진 B(카페24), 트렌드어워드(메일리) |
| 확인 불가 | 롱블랙, 무신사 매거진 (봇 차단) |

- 노션 기반 사이트는 표본에서 찾지 못했다
- 어피티(WordPress + 스티비)가 이 프로젝트의 v0.7 구조와 가장 비슷하다

---

## 변경 이력

| 날짜 | 버전 | 변경 내용 |
|---|---|---|
| 2026-09-16 | v0.1 | 최초 작성 (Google Docs). 뼈대 구성, 확인된 사실 정리, 미확정 사항 7건 등록 |
| 2026-09-16 | v0.2 | 4장 범위 작성. 검색·조회수를 포함으로 변경, 조회수는 노션 되돌려쓰기 방식으로 결정. 6장 기능 번호 부여. 부록 B 추가 |
| 2026-09-16 | v0.3 | 저장 위치를 로컬 마크다운(`docs/PRD.md`)으로 이전. 1·2장 작성, 3·5·6·7장 초안 작성. 7.5(유지보수) 신설 |
| 2026-09-16 | v0.4 | 작성자·클라이언트 정보 기입. 범위 축소 — 사이트 내 검색, 조회수 집계, 광고주 사용자 그룹(3.3)을 제외 |
| 2026-09-16 | v0.5 | 2.1 성공 기준 기입 (조회수 1,000 / 광고 문의 10% 증가). **사이트 내 검색과 조회수 집계를 다시 포함으로 이동** — 4.3 메모, F-04·F-09, 관련 리스크·미확정 사항 복원. 기능 번호를 F-01~F-13으로 재정리. 도메인 비용 부담을 작성자로, 스티비 비용 부담을 미정으로 변경. Q-16(목표치 기준), Q-17(스티비 비용 부담) 추가 |
| 2026-09-16 | v0.6 | **실제 콘텐츠 14건을 실측(부록 D)하고 그 결과로 미디어 처리 방식을 새로 정함(4.4).** 영상이 콘텐츠의 1/3이라는 사실 반영 — F-02에 영상 재생 추가. 노션 무료 5MB 한도에 콘텐츠 43%가 걸려, 1단계는 미디어를 구글 드라이브에 두고 2단계에서 노션 유료로 통합하기로 결정. 이미지 최적화를 범위에 추가(F-07), 영상 재인코딩은 제외. 기능 번호를 F-01~F-14로 재정리. 부록 C에 드라이브 폴더 속성 추가. Q-21~Q-25 추가 |
| 2026-09-19 | v0.8 | **2026-09-18 클라이언트 미팅 결과 반영.** 뉴스레터를 "사이트 글 링크 발송"에서 **별도로 쓰는 독립 콘텐츠(주 1~2회)**로 바꿈 — 5.1을 사이트 글(A)·뉴스레터(B) 두 흐름으로 재작성, F-08 수정, F-14 재정의 대기, 4.2·8장의 자동 발송·WordPress 뉴스레터 기각 근거 갱신. 사이트 글 형식(글자 없는 사진 + 텍스트, 4.3) 기록. Premium 결제·Spiel 테마 합의 반영. **DNS를 WordPress.com으로 옮긴 사실 반영**(8·9장, 7.2, 부록 B) — `contact@` 수신이 끊긴 상태임을 명시. 클라이언트의 댓글·커뮤니티 요청을 2.2·4.2에 표시. Q-10·Q-17·Q-27·Q-31 해결, Q-13·Q-16 보류, Q-28·Q-29·Q-30 갱신, Q-32·Q-33 신설 |
| 2026-09-25 | v0.9 | **2026-09-19~25 운영 중 결정 반영.** 댓글·제보 게시판을 비회원 방식으로 포함(2.2·4.1·4.2, F-16·F-17, Q-33 해결). **카카오톡 공유 버튼 추가**(F-15, 8장 공유 행 — v0.7에서 뺐던 것을 되살림). 업무 메일을 Google Workspace로 확정(8·9장, 부록 B, Q-28 해결). 글 형식 확정 — 사진 없는 글자 커버, **카드별 출처 표기**(4.3). 발행 절차에 좋아요·공유 끄기 추가(5.1), 운영 인계안(ChatGPT + WordPress.com 앱) 기록. 실제 화면 구성과 구현 현황 기록(5.2, 6장). iOS 실기기 확인 원칙(7.1), 카카오톡 카드 실측(7.2), 방침 개정·동의 분리(7.4), 비밀값 예외·저장소 밖 CSS·계정 인계(7.5). 리스크 6건 추가(10장). 네이버 블로그 운영 사실(1장·3.1), `뉴스` 카테고리 추가. **발행 주기를 사이트 문구 기준(카드뉴스 매일, 뉴스레터 매주 금요일)으로 수정**(3.1·5장·8·10장). Q-34~Q-39 신설. 부록 A.1에 카카오·ChatGPT·스티비 API·인스타그램 내보내기 공식 출처 추가 |
| 2026-09-18 | v0.7.1 | **WordPress.com 영상 업로드 서술을 바로잡았다** — VideoPress(전용 저장소 250GB)는 Premium이 아니라 Business부터이고, 어제 적은 "공식 문서끼리 어긋난다"는 오독이었다. Premium 13GB는 영상까지 공용으로 쓴다. 요금제 월 결제가 누락돼 보완. 10장 저장 공간 리스크를 Personal·Premium 양쪽 계산으로 다시 씀. 부록 C·8장의 "v0.6에 남아 있다"를 커밋 `6959fac`(v0.5) 참조로 정정 |
| 2026-09-17 | v0.7 | **직접 개발(Next.js + 노션 + 구글 드라이브)에서 WordPress.com + 스티비로 전환.** 클라이언트의 실사용 편의를 우선함. 뉴스레터는 스티비 수동 발송. F-05·F-06 삭제, 나머지 기능의 구현 방식 갱신. 8장 재작성(근거 칸은 작성자 작성 예정). 스티비 이메일 API 요금제 서술 오류 수정. 도메인 구매·업무 메일 설정 현황 반영. 카카오톡 공유 미리보기 언급 삭제. 부록 C 폐기, 부록 E(매체 CMS 조사) 추가. Q-03·Q-04·Q-05 해결, 노션·드라이브 관련 Q 7건 폐기, Q-26~Q-30 추가 |
